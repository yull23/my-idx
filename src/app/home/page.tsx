export default function Home(props: Props) {
  return (
    <div>
      <h1>Generate .VSC</h1>
      {/* <button onClick={handleClick}></button> */}
    </div>
  );
}

interface Props {
  // define props here
}
